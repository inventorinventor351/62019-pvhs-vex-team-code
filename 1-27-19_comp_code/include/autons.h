#ifndef AUTONS_H
#define AUTONS_H

void blueCap(); // blue capside, flips two caps then parks
void redFlag(); // red flagside, flips one cap, shoots two flags, hits bottom flag, flips another cap, shoots two more flags, hits another bottom flag
void redCap(); // red capside, flips two caps then parks
void blueFlag(); // blue flagside, flips one cap, shoots two flags, hits bottom flag, flips another cap, shoots two more flags, hits another bottom flag

#endif